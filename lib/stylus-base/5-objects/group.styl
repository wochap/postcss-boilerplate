//  Group
//  ==========================================================================

object('group', @(){
  display: flex
  flex-direction: row
  align-items: stretch

  element('fit', @(){
    flex: 1 1 0
    min-width: 0
    min-height: 0
  })
})
