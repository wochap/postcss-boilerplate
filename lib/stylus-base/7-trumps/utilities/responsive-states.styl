//  Responsive States
//  ==========================================================================
//
// 	doc: http://www.basscss.com/docs/responsive-states/


//  Show
//  --------------------------------------------------------------------------


utility('show', @(){
  for $key, $value in $breakpoints {
    +hasta($key) {
      suffix($key, @(){
        display: none !important
      })
    }
  }
})


//  Hide
//  --------------------------------------------------------------------------

utility('hide', @(){
  for $key, $value in $breakpoints {
    +desde($key) {
      suffix($key, @(){
        display: none !important
      })
    }
  }
})

utility('display-none', @(){ display: none !important })

utility('hide', @(){
  clip: rect(1px, 1px, 1px, 1px);
  height: 1px;
  overflow: hidden;
  position: absolute !important;
  width: 1px;
})
