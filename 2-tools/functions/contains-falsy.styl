/// Checks if a list does not contains a value.
///
/// @access private
///
/// @param {List} $list
///   The list to check against.
///
/// @return {Bool}

contains-falsy($vals)
  for $val in $vals
    if $val == null
      error('you can not use null, use false')
    if $val == false || $val == 'false'
      return true
  return false
