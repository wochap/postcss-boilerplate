/// Checks if a list does not contains a value.
///
/// @access private
///
/// @param {List} $list
///   The list to check against.
///
/// @return {Bool}

contains-falsy($hash)
    for $item in $hash
        if $item == null
            return true
    return false
