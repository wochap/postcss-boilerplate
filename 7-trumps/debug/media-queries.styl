//  Media queries debug
//  ==========================================================================
//
// it shows you where you are screen size


if $debug-media-queries
  body
    &::after
    &::before
      content: 'xs'
      font-size: 10rem
      left: 50%
      opacity: .25
      position: absolute
      top: 50%
      transform: translate3d(-50%, -50%, 0)

  for $key, $value in $breakpoints
    +desde($key)
    body
      &::after
      &::before
        content: $key
